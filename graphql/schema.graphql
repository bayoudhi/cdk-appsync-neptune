type User {
  id: ID!
  name: String!
}

input UserInput {
  name: String!
}

type Query {
  listUsers: [User!]!
}

type Mutation {
  createUser(user: UserInput!): User!
}

# type Subscription {
#   onCreatePost: Post @aws_subscribe(mutations: ["createPost"])
# }
